# Design notes

Archive for issue design notes and explain-diff reports from the dev pipeline. These are generated by the Claude workflow harness (now in `agent-harness`) and committed when issues ship.

Both files open automatically when the skill produces them. The links point at **GitHub Pages**
(`marcintk.github.io/ha-card-shared/design-notes/…`), which serves the committed `.html`
rendered — GitHub itself shows a linked `.html` file as source, not a page. Pages deploys from
`main` `/docs`, so this folder (`docs/design-notes/`) is served under `/design-notes/`;
`docs/.nojekyll` stops Jekyll from reprocessing the files.

| Issue | Note | Explain-diff | Status | PR  |
| ----- | ---- | ------------ | ------ | --- |

_No notes yet. As issues ship, design notes and explain-diff reports will appear here._
